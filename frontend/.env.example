# Frontend (Vercel) — copy to .env.local for local overrides
# In production, set this in the Vercel project Environment Variables.

# Render backend public URL (no trailing slash), e.g.:
# VITE_API_BASE=https://dine-ai-api.onrender.com
VITE_API_BASE=/api
