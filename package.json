{
  "name": "dine-ai",
  "private": true,
  "description": "Dine.AI — Vercel builds the React frontend only; API runs on Render",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
